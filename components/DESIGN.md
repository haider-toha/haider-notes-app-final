# Lined notebook

The notebook is the main site. `/` and `/notebook` start with the supplied closed cover. Click, tap, drag left or keyboard activation opens about me, even when another position is saved. Direct note links bypass the cover. `/contents` opens the two preliminary contents leaves; the contents link remains at the top of every reading page. `/all`, existing folder URLs, and published note URLs open reading pages.

The open notebook is the entire visible interface. White desk, ivory ruled paper, a narrow binding, stacked paper edges, blue-black ink (#303f53), and understated blue links (#355b78). Desktop has no toolbar or footer. Mobile turns by dragging the paper edges, with a previous link and no next button. No font picker, decorative corner folds, or hover animation.

Reenie Beanie supplies the handwriting. Notebook Math is a matching OFL derivative that adds missing Greek and mathematical symbols; both fonts and their licenses are served locally. Body text uses 24px type on a 29px ruling (21px on compact desktop, 23px on mobile); the opening title uses 62px (48px on mobile). Page numbers always use 12px system sans-serif with lining, tabular numerals.

Desktop shows two pages in a spread up to 1080px wide. Their standard height is 720px, capped to available desk height on short viewports so the paper bottom and controls remain visible. The `.is-mobile` layout applies below 701px wide or on coarse-pointer screens at most 500px tall, including phone landscape. Its actual engine height fits the available viewport and is capped at 1.85 times page width. It keeps 23px handwriting rather than shrinking an entire spread. Other portrait engine layouts retain their existing geometry. Keep the responsive engine minimum-width override: removing it causes horizontal overflow. Long passages scroll within the ruled writing area without changing shared source pagination.

`NotebookMobile.css` scopes every phone appearance rule to `.lined-notebook.is-mobile`. Desk padding is 18px top, 20px right, 30px bottom, and 32px left plus safe-area insets; the leaf uses 24px/22px/54px padding. A 20px neighboring-paper sliver carries real adjacent ink beside the binding crease. Nearly square bound corners and the visible bottom/right stacks make the focused leaf part of a book. The left stack supports the neighboring sliver. Preserve the full outline and folio inside the viewport. See [mobile research and decisions](../docs/mobile-notebook.md).

Deliberate outer-edge dragging/tapping turns a soft sheet across the spine. StPageFlip 2.0.7 supplies the fold geometry, reverse face, and moving shadows (950ms release animation, 0.28 maximum shadow opacity). The lifecycle adapter draws only when engine inputs change or an animation is active, and cancels frames and late event callbacks on unmount. Hover keeps the page flat. Reduced motion changes pages immediately.

Desktop's invisible 24px-wide full-height outer-edge controls retain accessible labels and keyboard focus. Mobile uses full-height 20px side grips and 44px corner areas for finger-driven folds, with a previous link and no next button. The central page number retains its 12px system type. Arrow keys turn pages when the spread has focus; they never intercept links. Hidden pages are inert. Page position remains available to screen readers. Content is authored in `constants.tsx`; there are no scratchpads or browser-local drafts. Every content hyperlink displays ↗ and opens in a new tab, including links to other notes.

Mobile writing owns native vertical scrolling; equations and tables can pan horizontally, and browser pinch remains available. General book, writing, and loose-sheet touch handling stays `auto`; only side grips and the loose-sheet header claim drag gestures. Fold from the side grips and detach with a deliberate outward pull and hold on the right grip. A vertical gesture over prose must not turn or detach paper. Detached prose remains scrollable. Engine geometry updates wait until the active fold or captured gesture finishes.

Reading pages combine consecutive paragraphs, with blank lines between them. Keep the opening identity page; balance the final passages to avoid a nearly empty final reading page. Preserve the 24px handwriting and 29px rule rhythm rather than shrinking type. Side grabs map pointer displacement from an outer-boundary anchor, preventing an oversized fold at the first movement.

A removable sheet uses outward rubber-band resistance followed by a damped spring release. The implementation uses unit mass, translational stiffness 240/damping 29, angular stiffness 115/damping 16, bounded frame time, and velocity-dependent torque around the grab position. Reduced motion removes the spring delay and unfurl effect. Heading alignment, padding, background and scroll position are captured from the source rather than recomposed. Reveal real underlying-page content during removal. The sheet returns by aligning its binding edge to the spine, with no extra recovery control.

Opening reading page: a 62px Reenie Beanie name (48px on mobile), location immediately below on a 29px line, then social links after one blank ruled line. After a two-rule gap below the social links, place the original opening paragraph about current work. Maintain generous margins; move subsequent paragraphs to the following pages without repetition. No running-heading label, repeated name, tagline, or contents list on the identity leaf. Preserve the empty running-head element's geometry because detached-sheet presentation reads that element.

Paper details stay still at rest. A completed turn gets a 310ms flex of less than one degree on the inner article; the renderer retains sole ownership of leaf transforms. Cut edges appear along the bottom and outer sides. Read and unread stacks range from 1–13px with reading progress, with final-spread alignment on desktop. Reverse ink is mirrored from the opposite face, aria-hidden, noninteractive and faint (about 2% at rest, 5–6% while lifting). No effect introduces a separate layer that captures grabs.

Contents occupies two actual preliminary leaves in the same page-turn engine, numbered i and ii. They list all 27 notes in folder groups, split 14/13, with a repeated group heading where a group continues. Show “contents” only on the left leaf; one 29px ruled-line space precedes the right-hand blog continuation. Use plain handwriting directly on the existing paper: a 42px/58px contents title, 32px/29px group headings, indented 22px/29px desktop entries and 44px mobile touch rows. Separate groups by one 29px ruled line, omit dotted leaders, and write reference page numbers in the same 22px handwriting. The contents leaves keep their 29px running-head row for the navigation control and use a 9px gap before the contents ink. Desktop contents fits the remaining writing area; mobile contents scrolls within its bounded leaf when needed. A small handwritten control inside the left desktop page’s upper outer margin, or each mobile page’s upper left margin, reads “← contents” and navigates to `/contents` during reading; on the preliminary leaves, “continue reading →” restores the remembered page and its canonical note URL. Each control belongs to its sheet and turns with the paper, when that face has a control; never position one shared control over the spread. Desktop shows one control on the left page of each spread; mobile keeps one control on every page at the upper left. The control shares the running-head row: reserve room beside long note headings and retain a 44px touch target on mobile. Contents has a real route and browser-history entry; the cover opens to about me. No ribbon, protruding tab, popup or separate contents surface remains. Reading pages retain numbering from 1; route and storage indices exclude the two preliminary leaves. Store the remembered reading position as a note ID and source offset, preserving it through repagination and visits to contents.

Expanded media uses a paper-colored accessible dialog with pinned ink controls. Close, zoom and reset stay outside the transformed drawing; keyboard focus stays inside the dialog and returns to the opener. Background pages are inert while the viewer is open.

The notebook includes all source notes through `notebookPages.ts`. Pagination preserves every source character and keeps code fences, diagrams, simulations, equations and tables intact. Estimate rendered line height and media aspect ratios, split long prose at safe boundaries, keep headings with the following content, and balance sparse final leaves. Section titles wrap in full; do not truncate them or append “continued.” Markdown section dividers remain in the source audit but have no visible notebook rendering. `NotebookContent` shares the original note renderer. Tables match the handwriting and ink at 24px desktop/23px mobile, with bold headers and subtle horizontal rules; wide phone tables pan within a focusable wrapper. Code retains readable monospace typography. Only nearby and destination pages mount rich content; simulations run on visible pages. Expanded images and diagrams render outside the transformed leaves. Notebook diagrams use Mermaid’s hand-drawn look, transparent backgrounds, blue-black ink, and the same explicit Reenie Beanie font as the page. Keep source topology and labels intact. Every complete diagram fits the page width and a 450px height cap, with no panning or cropping required in the notebook. Expansion offers closer reading. Geometry metadata estimates page packing, while the SVG itself renders live; regenerate it with `scripts/measure-notebook-diagrams.mjs` after changing diagram sources or typography.

All equations and simulation labels use Notebook Math, including Greek and operators. Keep every emitted glyph covered by the font generator and glyph audit; never silently substitute typeset glyphs. Simulations retain transparent canvas backgrounds and their original numerical dynamics.

The first mobile contents leaf shows a full-width open cover left of the hinge, cropped by the viewport rather than framed around the paper, with no invented preceding-page ink or read stack. Subsequent leaves restore the neighboring paper. Desktop and mobile ruling use a local background attachment so the lines travel with the writing during scrolling.

The closed cover extends the existing physical book: supplied burgundy/gold artwork, unchanged 3:4 proportions, white desk, restrained cut-paper edge and shadow, and a handwritten open invitation. Responsive WebP assets retain the PNG master. The cover and revealed book share a moving hinge while opening; canceled pulls settle closed. It stays outside the engine's indexed leaves. Contents selections use up to five real folds, sampling long distances with a 160–750ms distance-based duration. Interrupted journeys publish only the settled position; history restoration remains immediate.
